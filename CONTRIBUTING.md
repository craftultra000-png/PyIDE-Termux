# المساهمة في PyIDE Termux Pro

نرحب بالتحسينات التي تجعل بيئة Python على الهاتف أوضح وأخف وأكثر أماناً. قبل إرسال تغيير، شغّل الاختبارات المحلية عبر `bash tests/run-tests.sh` وتأكد من أن الواجهة لا تفترض شاشة مكتبية فقط.

| نوع التغيير | المعيار المطلوب |
|---|---|
| منطق الملفات | يجب أن يبقى محصوراً داخل الجذور المسموحة وأن يتعامل مع المسارات الحقيقية لا النصية فقط. |
| واجهة المستخدم | يجب أن تعمل باللمس، وتبقى المسارات والشيفرة والمخرجات باتجاه LTR حتى عند اللغة العربية. |
| الترجمة | أضف المفتاح إلى الإنجليزية أولاً، ثم العربية، واحرص على توفير ترجمة في اللغات الأخرى عند الإمكان. |
| وحدة JavaScript جديدة | ضعها في `static/js/core` للمرافق المجردة أو `static/js/components` للمكوّنات المرئية، وأضف اختباراً لمنطقها النقي. |

يفضل أن يكون كل طلب دمج صغيراً ومحدداً، مع وصف للمشكلة وخطوات تجربة التغيير من الهاتف. لا تضف مكتبات خارجية ما لم تكن فائدتها واضحة وقابلة للتشغيل داخل Termux.
